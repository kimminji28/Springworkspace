spring.application.name=sp04

server.servlet.context-path=/myApp

# datasource : DBMS, Oracle
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.datasource.url=jdbc:oracle:thin:@localhost:1521:xe
spring.datasource.username=hr
spring.datasource.password=hr

# Mybatis(Bean -> name) Config
# settings => Mybatis.comfiguration
mybatis.configuration.map-underscore-to-camel-case=true
mybatis.configuration.jdbc-type-for-null=NULL

# typeAliases => mybatis.type-aliases-XXX
mybatis.type-aliases-package=com.yedam.app

# typeHandler => boot : auto configuration

# mappers => mybatis.mapper-locations
mybatis.mapper-locations=classpath:mapper/*.xml

# log
logging.level.com.yedam.app=debug

# ViewResolver
#spring.thymeleaf.prefix=templates
#spring.thymeleaf.suffix=html

# MultipartResolver Setting
# multipart => beanName
spring.servlet.multipart.file-size-threshold=0B
spring.servlet.multipart.location=C:/temp
spring.servlet.multipart.max-file-size=1MB
spring.servlet.multipart.max-request-size=10MB

file.upload.path=D:/upload/